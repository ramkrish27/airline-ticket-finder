\"\"\"Real-time flight search aggregation engine\"\"\"\n\nimport asyncio\nfrom typing import List, Dict, Any, Optional\nfrom datetime import datetime\nimport uuid\nimport logging\nfrom concurrent.futures import as_completed\n\nlogger = logging.getLogger(__name__)\n\n\nclass SearchEngine:\n    \"\"\"\n    Aggregates flight search results from multiple providers in real-time.\n    Handles concurrent searches and result deduplication.\n    \"\"\"\n\n    def __init__(self, connectors: List[Any], cache_manager: Optional[Any] = None):\n        \"\"\"\n        Initialize the search engine.\n        \n        Args:\n            connectors: List of flight search connectors\n            cache_manager: Optional cache manager for caching results\n        \"\"\"\n        self.connectors = connectors\n        self.cache_manager = cache_manager\n        self.max_concurrent_searches = 5\n        self.search_timeout = 30\n\n    async def search(\n        self,\n        departure_airport: str,\n        arrival_airport: str,\n        departure_date: str,\n        return_date: Optional[str] = None,\n        passengers: int = 1,\n        cabin_class: str = \"economy\"\n    ) -> Dict[str, Any]:\n        \"\"\"\n        Search for flights across all connectors concurrently.\n        \n        Args:\n            departure_airport: IATA code of departure airport\n            arrival_airport: IATA code of arrival airport\n            departure_date: Departure date (YYYY-MM-DD format)\n            return_date: Return date for round trips (optional)\n            passengers: Number of passengers\n            cabin_class: Cabin class (economy, business, first)\n            \n        Returns:\n            Dictionary with aggregated search results and metadata\n        \"\"\"\n        search_id = str(uuid.uuid4())\n        start_time = datetime.now()\n        \n        try:\n            # Create concurrent search tasks\n            search_tasks = []\n            for connector in self.connectors:\n                task = asyncio.create_task(\n                    self._search_connector(\n                        connector,\n                        departure_airport,\n                        arrival_airport,\n                        departure_date,\n                        return_date,\n                        passengers,\n                        cabin_class\n                    )\n                )\n                search_tasks.append(task)\n            \n            # Wait for all searches with timeout\n            results = await asyncio.wait_for(\n                asyncio.gather(*search_tasks, return_exceptions=True),\n                timeout=self.search_timeout\n            )\n            \n            # Aggregate results\n            all_flights = []\n            for result in results:\n                if isinstance(result, list):\n                    all_flights.extend(result)\n                elif isinstance(result, Exception):\n                    logger.warning(f\"Search error: {result}\")\n            \n            # Deduplicate and sort flights\n            flights = self._deduplicate_flights(all_flights)\n            flights = self._sort_flights(flights)\n            \n            execution_time = (datetime.now() - start_time).total_seconds()\n            \n            result = {\n                \"search_id\": search_id,\n                \"status\": \"completed\",\n                \"flights\": flights,\n                \"total_results\": len(flights),\n                \"providers\": len(self.connectors),\n                \"execution_time\": execution_time,\n                \"timestamp\": start_time.isoformat()\n            }\n            \n            # Cache result if cache manager is available\n            if self.cache_manager:\n                await self.cache_manager.set(search_id, result)\n            \n            logger.info(\n                f\"Search completed: {search_id}, \"\n                f\"{len(flights)} flights from {len(self.connectors)} providers, \"\n                f\"Time: {execution_time:.2f}s\"\n            )\n            \n            return result\n            \n        except asyncio.TimeoutError:\n            logger.error(f\"Search timeout: {search_id}\")\n            return {\n                \"search_id\": search_id,\n                \"status\": \"timeout\",\n                \"flights\": [],\n                \"error\": \"Search exceeded maximum time limit\"\n            }\n        except Exception as e:\n            logger.error(f\"Search aggregation failed: {e}\")\n            return {\n                \"search_id\": search_id,\n                \"status\": \"error\",\n                \"flights\": [],\n                \"error\": str(e)\n            }\n\n    async def _search_connector(\n        self,\n        connector: Any,\n        departure_airport: str,\n        arrival_airport: str,\n        departure_date: str,\n        return_date: Optional[str],\n        passengers: int,\n        cabin_class: str\n    ) -> List[Dict[str, Any]]:\n        \"\"\"\n        Execute search on a single connector.\n        \n        Args:\n            connector: Flight search connector\n            departure_airport: IATA code of departure airport\n            arrival_airport: IATA code of arrival airport\n            departure_date: Departure date\n            return_date: Return date (optional)\n            passengers: Number of passengers\n            cabin_class: Cabin class\n            \n        Returns:\n            List of flights from connector\n        \"\"\"\n        try:\n            logger.info(f\"Searching {connector.name}...\")\n            flights = await connector.search(\n                departure_airport=departure_airport,\n                arrival_airport=arrival_airport,\n                departure_date=departure_date,\n                return_date=return_date,\n                passengers=passengers,\n                cabin_class=cabin_class\n            )\n            logger.info(f\"{connector.name} returned {len(flights)} flights\")\n            return flights\n        except Exception as e:\n            logger.error(f\"{connector.name} search failed: {e}\")\n            return []\n\n    def _deduplicate_flights(self, flights: List[Dict[str, Any]]) -> List[Dict[str, Any]]:\n        \"\"\"\n        Remove duplicate flights from results.\n        Flights are considered duplicates if they have the same airline,\n        departure time, and arrival time.\n        \n        Args:\n            flights: List of flight dictionaries\n            \n        Returns:\n            Deduplicated list of flights\n        \"\"\"\n        seen = set()\n        unique_flights = []\n        \n        for flight in flights:\n            key = (\n                flight.get(\"airline\"),\n                flight.get(\"departure_time\"),\n                flight.get(\"arrival_time\")\n            )\n            \n            if key not in seen:\n                seen.add(key)\n                unique_flights.append(flight)\n        \n        return unique_flights\n\n    def _sort_flights(self, flights: List[Dict[str, Any]]) -> List[Dict[str, Any]]:\n        \"\"\"\n        Sort flights by price (ascending).\n        \n        Args:\n            flights: List of flight dictionaries\n            \n        Returns:\n            Sorted list of flights\n        \"\"\"\n        return sorted(flights, key=lambda x: x.get(\"price\", float(\"inf\")))\n\n    async def get_result(self, search_id: str) -> Optional[Dict[str, Any]]:\n        \"\"\"\n        Retrieve cached search results.\n        \n        Args:\n            search_id: The search result ID\n            \n        Returns:\n            Cached search result or None\n        \"\"\"\n        if self.cache_manager:\n            return await self.cache_manager.get(search_id)\n        return None\n\n    async def health_check(self) -> Dict[str, Any]:\n        \"\"\"\n        Check health of all connectors.\n        \n        Returns:\n            Dictionary with connector health status\n        \"\"\"\n        health_status = {}\n        \n        for connector in self.connectors:\n            try:\n                is_healthy = await connector.health_check()\n                health_status[connector.name] = \"healthy\" if is_healthy else \"unhealthy\"\n            except Exception as e:\n                logger.error(f\"Health check failed for {connector.name}: {e}\")\n                health_status[connector.name] = \"error\"\n        \n        return health_status\n"